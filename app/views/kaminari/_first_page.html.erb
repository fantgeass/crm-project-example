<% if current_page.first? %>
  <li class="active">
    <%= link_to raw(t 'views.pagination.first'), '#', :remote => remote, title: 'Первая' %>
  </li>
<% else %>
  <li>
    <%= link_to raw(t 'views.pagination.first'), url, :remote => remote, title: 'Первая' %>
  </li>
<% end %>
